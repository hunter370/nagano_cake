<h1>商品一覧(全?件)</h1>

<% @items.each do |item| %>

<%= link_to items_show_path(item.id) do %>
 <%= image_tag item.get_image(100,100) %>
<% end %>

<%= item.name %>

¥<%= item.price %>

<% end %>

<%= paginate @items %>
